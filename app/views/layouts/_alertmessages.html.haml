- flash.each do |key, value|
  %div{:class => "alert alert-#{key}"}
    %ul
      %li
        = value
  -# - if key= "error" || "alert"
    -# %a.close{"data-dismiss" => "alert", :href => "#"} ×
  -# - elsif key= "success" || "notice"
  :javascript
    window.setTimeout(function() {
    $(".alert").fadeTo(500, 0).slideUp(500, function(){
        $(this).remove();
    });
    }, 2000);
